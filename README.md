# chatkad
chat application
ChatKad is a multiroom chat server application.It lets a user create a chat room and also lets them join multiple 
chatrooms.
The server is hosted on heroku.
Database used is MongoDB.
Socket.IO is used for sending and updating messages.
